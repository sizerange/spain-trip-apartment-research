{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "import:github-research": "tsx ./src/import-github-research.ts",
    "test": "node --test src/publication-input.test.mjs import-schedule.test.mjs",
    "typecheck": "pnpm run test && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
